server:
  port: 8093
  servlet:
    context-path: /product

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:password@localhost:8761/eureka
spring:
  application:
    name: product-service
