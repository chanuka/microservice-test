apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-notification.fullname" . }}-test-connection"
  labels:
    {{- include "my-notification.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-notification.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
