apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-zipkin.fullname" . }}-test-connection"
  labels:
    {{- include "my-zipkin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-zipkin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
