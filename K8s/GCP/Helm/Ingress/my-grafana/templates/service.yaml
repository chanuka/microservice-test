apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-grafana.fullname" . }}
  labels:
    {{- include "my-grafana.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-grafana.selectorLabels" . | nindent 4 }}
