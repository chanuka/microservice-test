server:
  port: 8092
  servlet:
    context-path: /order

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:password@localhost:8761/eureka
spring:
  application:
    name: order-service

